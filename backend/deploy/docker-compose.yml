services:
  randomly-roll-backend:
    build:
      context: ..
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ../.env
    ports:
      - "3000:3000"
    volumes:
      - ../storage:/app/storage
      - ../prisma:/app/prisma
